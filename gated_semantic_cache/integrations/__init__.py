"""Framework integrations for GatedSemanticCache.

Each submodule imports its third-party dependency lazily and is intentionally NOT
imported by the top-level package, so the core install stays dependency-light.
"""
